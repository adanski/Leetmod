// If enabled, players can select from all the 3D player models that are available in multiplayer.
// There are 30 different models. 15 models for each opposite sides (teams).
// Note: If the model precaching limit is reached on a custom map, disable this setting
// so that you can play on them.
// 0 = Disabled (default)
// 1 - Free-based gametypes only
// 2 - Team-based gametypes only
// 3 - All gametypes
// set scr_cap_allow_othermodels "0"

// This feature is removed because compiling the mod to support this, increases its size
// to > 8MB (and these models are already available in stock installation, they shouldn't
// be included again when compiling :/ )